# BMM-Triad Module Installer Configuration
# Condensed 3-agent architecture for simpler projects

name: bmm-triad
display_name: 'BMM Triad'
description: 'Streamlined 3-agent architecture condensing 16 BMM agents into Strategist Marketer, Technologist Architect, and Fulfillization Manager'
version: '1.0.0'
author: 'BMad'

# Module metadata
tags:
  - methodology
  - product-development
  - agile
  - simplified

# Dependencies
requires:
  - core # BMAD Core is required

# Conflicts (can't use both simultaneously per project)
conflicts_per_project:
  - bmm # User should choose one methodology per project

# Installation configuration
install:
  # Copy these directories
  directories:
    - agents
    - workflows
    - teams
    - data

  # Generate config.yaml with these questions
  config_questions:
    - field: project_name
      prompt: 'What is your project name?'
      type: string
      required: true

    - field: user_name
      prompt: 'What is your name?'
      type: string
      required: true

    - field: communication_language
      prompt: 'Preferred communication language?'
      type: string
      default: 'English'

    - field: document_output_language
      prompt: 'Preferred document output language?'
      type: string
      default: 'English'

    - field: user_skill_level
      prompt: 'Your skill level?'
      type: choice
      options:
        - beginner
        - intermediate
        - advanced
      default: intermediate

    - field: planning_artifacts
      prompt: 'Where should planning artifacts be saved?'
      type: path
      default: '{project-root}/_bmad-output/planning-artifacts'

    - field: implementation_artifacts
      prompt: 'Where should implementation artifacts be saved?'
      type: path
      default: '{project-root}/_bmad-output/implementation-artifacts'

  # Register slash commands for the agents
  slash_commands:
    - name: 'bmad:bmm-triad:agents:strategist-marketer'
      description: 'Sage - Product Strategist + Market Intelligence Expert'
      agent: 'agents/strategist-marketer.md'

    - name: 'bmad:bmm-triad:agents:technologist-architect'
      description: 'Theo - Technical Architect + Implementation Specialist'
      agent: 'agents/technologist-architect.md'

    - name: 'bmad:bmm-triad:agents:fulfillization-manager'
      description: 'Finn - Delivery + Experience + Operations Specialist'
      agent: 'agents/fulfillization-manager.md'

  # Register workflow commands
  workflow_commands:
    - name: 'bmad:bmm-triad:workflows:triad-discovery'
      description: 'Discovery workflow - Strategy to Requirements'
      workflow: 'workflows/triad-discovery/workflow.md'

    - name: 'bmad:bmm-triad:workflows:triad-build'
      description: 'Build workflow - Architecture to Implementation'
      workflow: 'workflows/triad-build/workflow.md'

    - name: 'bmad:bmm-triad:workflows:triad-ship'
      description: 'Ship workflow - Testing to Delivery to Docs'
      workflow: 'workflows/triad-ship/workflow.md'

    - name: 'bmad:bmm-triad:workflows:triad-full-cycle'
      description: 'Full cycle orchestration - Discovery to Build to Ship'
      workflow: 'workflows/triad-full-cycle/workflow.md'

# Post-install message
post_install_message: |
  🏗️ BMM-Triad installed successfully!

  Your 3-agent team is ready:
    📊 Sage (Strategist Marketer) - Business WHY & WHO
    🔧 Theo (Technologist Architect) - Technical HOW
    🎯 Finn (Fulfillization Manager) - SHIP & EXPERIENCE

  Quick Start:
    /bmad:bmm-triad:agents:strategist-marketer  - Start with strategy
    /bmad:bmm-triad:workflows:triad-full-cycle  - Run complete cycle

  Need the full 16-agent BMM? Install bmm module alongside.
